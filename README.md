Clone this repo somewhere *i put it in my $HOME* ```cd ~/
git clone https://github.com/EnderSpy29/Ender-zsh.git```

symlink the .zshenv to your $HOME `ln -s Ender-zsh/.zshenv .zshenv`

symlink the git repo to your .config/ ```cd ~/.config
ln -s ~/Ender-zsh zsh```
